<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Donate to Help Save Lives</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f0f4f8;
        }
        .hero {
            position: relative;
            background-image: url('/images/background.jpg'); /* Static local path to the image */
            background-size: cover;
            background-position: center;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            color: white;
            text-align: center;
        }
        .hero-text {
            font-size: 2.5rem;
            font-weight: bold;
            margin-bottom: 20px;
        }
        .donation-box {
            position: absolute;
            bottom: 20px;
            left: 20px;
            background: #ffffff;
            border-radius: 10px;
            padding: 20px;
            box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
            max-width: 300px;
            width: 90%;
        }
        .donation-box input[type="number"] {
            width: 100%;
            padding: 10px;
            margin-bottom: 10px;
            border: 1px solid #ddd;
            border-radius: 5px;
        }
        .donation-box button {
            width: 100%;
            padding: 10px;
            background-color: #2d8f5e;
            color: white;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            cursor: pointer;
        }
        .donation-box button:hover {
            background-color: #24724b;
        }
    </style>
</head>
<body>
    <div class="hero">
        <div class="hero-text">
            DONATE TO HELP SAVE LIVES
        </div>
        <div class="donation-box">
            <form action="/donate" method="POST">
                <input type="number" name="amount" value="25" min="1" step="1" placeholder="Enter amount" required>
                <div>
                    <button type="button">$5</button>
                    <button type="button">$25</button>
                    <button type="button">$50</button>
                    <button type="button">$100</button>
                </div>
                <label>
                    <input type="checkbox" name="monthly">
                    Make this donation monthly
                </label>
                <button type="submit">Donate</button>
            </form>
        </div>
    </div>
</body>
</html>
