<!DOCTYPE html>
<html>

<head>
  <title>Edit Volunteer</title>
  <style>
    body {
      font-family: 'Roboto', sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      background-color: #f0f0f0;
      margin: 0;
    }

    .edit-form-container {
      background-color: #ffffff;
      padding: 2rem;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      width: 100%;
      max-width: 600px;
    }

    h1 {
      text-align: center;
      color: #7d8562;
      margin-bottom: 1rem;
    }

    form {
      display: flex;
      flex-direction: column;
    }

    label {
      margin-bottom: 0.5rem;
      font-weight: bold;
    }

    input,
    select {
      margin-bottom: 1rem;
      padding: 0.75rem;
      border: 1px solid #d0d0d0;
      border-radius: 4px;
      font-size: 1rem;
    }

    .button {
      padding: 0.75rem 1rem;
      font-size: 1rem;
      color: #ffffff;
      background-color: #28a745;
      border: none;
      border-radius: 4px;
      cursor: pointer;
    }

    .button:hover {
      opacity: 0.9;
    }
  </style>
</head>

<body>
  <div class="edit-form-container">
    <h1>Edit Volunteer</h1>
    <form action="/editVolunteer/<%= volunteer.volunteerid %>" method="POST">
      <label for="first_name">First Name</label>
      <input type="text" id="first_name" name="first_name" value="<%= volunteer.volfirstname %>" required>

      <label for="last_name">Last Name</label>
      <input type="text" id="last_name" name="last_name" value="<%= volunteer.vollastname %>" required>

      <label for="phone">Phone</label>
      <input type="tel" id="phone" name="phone" value="<%= volunteer.phone %>" required>

      <label for="email">Email</label>
      <input type="email" id="email" name="email" value="<%= volunteer.email %>" required>

      <label for="sewing_level">Sewing Level</label>
      <select id="sewing_level" name="sewing_level" required>
        <option value="Beginner" <%= volunteer.sewinglevel === "Beginner" ? "selected" : "" %>>Beginner</option>
        <option value="Intermediate" <%= volunteer.sewinglevel === "Intermediate" ? "selected" : "" %>>Intermediate</option>
        <option value="Advanced" <%= volunteer.sewinglevel === "Advanced" ? "selected" : "" %>>Advanced</option>
      </select>

      <label for="monthly_hours">Monthly Hours</label>
      <input type="number" id="monthly_hours" name="monthly_hours" value="<%= volunteer.monthlyhours %>" required>

      <label for="heard_about">Heard About Opportunity</label>
      <input type="text" id="heard_about" name="heard_about" value="<%= volunteer.heardaboutopportunity %>" required>

      <label for="zipcode">Zip Code</label>
      <input type="text" id="zipcode" name="zipcode" value="<%= volunteer.zipcode %>" required>

      <button type="submit" class="button">Save Changes</button>
    </form>
  </div>
</body>

</html>