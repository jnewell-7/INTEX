<!DOCTYPE html>
<html>

<head>
  <title>Edit Event Request</title>
  <style>
    body {
      font-family: 'Arial', sans-serif;
      background-color: #f9f9f9;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
    }

    .form-container {
      background-color: #ffffff;
      padding: 2rem;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      width: 90%;
      max-width: 600px;
    }

    h1 {
      color: #495057;
      font-weight: 700;
      margin-bottom: 1rem;
      text-align: center;
    }

    label {
      display: block;
      margin-bottom: 0.5rem;
      font-weight: bold;
      color: #495057;
    }

    input,
    select,
    textarea {
      width: 100%;
      padding: 0.5rem;
      margin-bottom: 1rem;
      border: 1px solid #ccc;
      border-radius: 4px;
    }

    input:focus,
    select:focus,
    textarea:focus {
      outline: none;
      border-color: #6b8e23;
      box-shadow: 0 0 5px rgba(107, 142, 35, 0.5);
    }
    .button {
      padding: 0.6rem 1rem;
      border: none;
      background-color: #6b8e23;
      color: white;
      font-size: 1rem;
      cursor: pointer;
      border-radius: 4px;
      text-align: center;
      margin-top: 2rem;
      margin-right: 1rem;
    }

    .button:hover {
      opacity: 0.9;
    }

    .button.cancel {
      padding: 0.6rem 1rem;
      font-size: 1rem;
      border: none;
      background-color: #8f9775;
      /* Olive green */
      color: white;
      cursor: pointer;
      border-radius: 4px;
      text-decoration: none;
    }

    .button.cancel:hover {
      opacity: 0.9;
    }

    .buttons-container {
      display: flex;
      justify-content: space-between;
      margin-top: 1rem;
    }
  </style>
</head>

<body>
  <div class="form-container">
    <h1>Edit Event Request</h1>
    <form action="/editReq/<%= request.requestid %>" method="POST">
      <label for="eventdate">Event Date</label>
      <input type="date" id="eventdate" name="eventdate"
        value="<%= new Date(request.eventdate).toISOString().split('T')[0] %>" required>

      <label for="eventtime">Event Time</label>
      <input type="time" id="eventtime" name="eventtime" value="<%= request.eventtime %>" required>

      <label for="address">Proposed Address</label>
      <input type="text" id="address" name="proposedeventaddress" value="<%= request.proposedeventaddress %>" required>

      <label for="city">City</label>
      <input type="text" id="city" name="city" value="<%= request.city %>" required>

      <label for="state">State</label>
      <input type="text" id="state" name="state" value="<%= request.state %>" required>

      <label for="zipcode">Zip Code</label>
      <input type="text" id="zipcode" name="zipcode" value="<%= request.zipcode %>" required>


      <button type="submit" class="button save">Save Changes</button>
      <a href="/manageEventRequests" class="button cancel">Cancel</a>
    </form>
  </div>
</body>

</html>