<!DOCTYPE html>
<html>

<head>
  <title>
    <%= title %>
  </title>
  <style>
    /* Global Styles */
    body {
      font-family: 'Arial', sans-serif;
      background-color: #f9f9f9;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
    }

    .form-container {
      background-color: #ffffff;
      padding: 2rem;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      width: 90%;
      max-width: 600px;
    }

    h1 {
      color: #495057;
      font-weight: 700;
      margin-bottom: 1rem;
      text-align: center;
    }

    label {
      display: block;
      margin-top: 20px;
      font-weight: bold;
      color: #495057;
    }

    input,
    select {
      width: 100%;
      padding: 10px;
      margin-top: 5px;
      border: 1px solid #ddd;
      border-radius: 4px;
      box-sizing: border-box;
    }

    input:focus,
    select:focus {
      outline: none;
      border-color: #6b8e23;
      box-shadow: 0 0 5px rgba(107, 142, 35, 0.5);
    }

    .button {
      padding: 0.6rem 1rem;
      border: none;
      background-color: #6b8e23;
      color: white;
      font-size: 1rem;
      cursor: pointer;
      border-radius: 4px;
      text-align: center;
      margin-top: 2rem;
      margin-right: 1rem;
    }

    .button:hover {
      opacity: 0.9;
    }

    .button.cancel {
      padding: 0.6rem 1rem;
      font-size: 1rem;
      border: none;
      background-color: #8f9775;
      /* Olive green */
      color: white;
      cursor: pointer;
      border-radius: 4px;
      text-decoration: none;
    }

    .button.cancel:hover {
      opacity: 0.9;
    }

    .buttons-container {
      display: flex;
      justify-content: space-between;
      margin-top: 1rem;
    }
  </style>
</head>

<body>
  <div class="form-container">
    <h1>
      <%= title %>
    </h1>
    <form action="/saveEvent" method="POST">
      <!-- Hidden input for requestid -->
      <input type="hidden" name="requestid" value="<%= event.requestid %>">

      <h2>Event Info</h2>

      <label for="eventdate">Event Date:</label>
      <input type="date" id="eventdate" name="eventdate"
        value="<%= new Date(event.eventdate).toISOString().split('T')[0] %>" required>

      <label for="eventaddress">Event Address:</label>
      <input type="text" id="eventaddress" name="eventaddress" value="<%= event.proposedeventaddress %>" required>

      <label for="zipcode">Zip Code:</label>
      <input type="text" id="zipcode" name="zipcode" value="<%= event.zipcode %>" required>

      <label for="totalparticipants">Total Participants:</label>
      <input type="number" id="totalparticipants" name="totalparticipants" min="0"
        placeholder="Enter actual number of participants" required>

      <h2>Produced Items</h2>

      <label for="pockets">Pockets Completed:</label>
      <input type="number" id="pockets" name="pockets" min="0" value="0">

      <label for="collars">Collars Completed:</label>
      <input type="number" id="collars" name="collars" min="0" value="0">

      <label for="envelopes">Envelopes Completed:</label>
      <input type="number" id="envelopes" name="envelopes" min="0" value="0">

      <label for="vests">Vests Completed:</label>
      <input type="number" id="vests" name="vests" min="0" value="0">

      <button type="submit" class="button save">Save Event</button>
      <a href="/admin" class="button cancel">Cancel</a>
    </form>
  </div>
</body>

</html>